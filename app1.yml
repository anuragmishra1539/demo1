spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/mydb
    username: postgres
    password: 1234
    driver-class-name: org.postgresql.Driver
    # other datasource-related properties

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
